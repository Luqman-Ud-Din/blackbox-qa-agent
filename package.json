{
  "name": "argus-qa",
  "version": "2.0.0",
  "description": "Argus — Autonomous UI Auditor Claude Code plugin. MCP-driven annotation, ADO bug filing, 66 detection/functional skills.",
  "author": "argus-qa",
  "license": "MIT",
  "keywords": [
    "argus",
    "qa",
    "testing",
    "playwright",
    "accessibility",
    "automation",
    "cross-browser",
    "claude-plugin",
    "claude-code"
  ],
  "files": [
    ".claude-plugin",
    "skills",
    "scripts",
    "test-cases",
    "README.md"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "pdf-parse": "^1.1.4",
    "playwright": "^1.40.0",
    "xlsx": "^0.18.5"
  },
  "optionalDependencies": {
    "@playwright/test": "^1.40.0"
  },
  "private": true,
  "_dependencyNote": "scripts/run-passive-probes.cjs runs ALL passive probes deterministically via the `playwright` package (the production fix for skill-collapse + fabrication). Requires `npx playwright install` for browser binaries. Interactive skills + route discovery still use MCP. @playwright/test stays optional (repair-bugs.cjs)."
}
